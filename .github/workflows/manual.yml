name: manual trigger
on:
  issues:
    types: [opened, edited, milestoned]
    inputs:
      foo: 'bar'
jobs:
  hello:
    name: hello
    runs-on: ubuntu-latest
    steps:
      - name: print hello
        run: echo "hello ${{ github.event.inputs.foo }}"